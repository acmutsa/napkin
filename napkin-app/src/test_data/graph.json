{
  "title": "Spring '26 Schema",
  "type": "object",
  "properties": {
  "nodes": {
    "Server": {
      "type": "ec2",
      "properties": {
        "id": "string",
        "name": "string",
        "instanceType": "string",
        "region": "string"
      },
      "ports": {
        "inputs": {
          "in-env": "data",
          "in-network": "network"
        },
        "outputs": {
          "out-network": "network",
          "out-data": "data"
        }
      }
    },
    "Database": {
      "properties": {
        "id": "string",
        "name": "string",
        "engine": {
          "type": "string",
          "enum": ["mysql", "postgres", "mongodb", "oracle"] 
        },
        "storageSize": "number",
        "region": "string"
      },
      "ports": {
        "inputs": {
          "in-network": "network"
        },
        "outputs": {
          "out-conn": "data"
        }
      }
    }
  },
  "edges": {
    "ServerToDatabase": {
      "source": {
        "node": "Server",
        "port": "out-data"
      },
      "target": {
        "node": "Database",
        "port": "in-network"
      },
      "type": "data-flow"
    }
  }
 }
}